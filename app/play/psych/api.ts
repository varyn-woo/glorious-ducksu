import { UserText_t } from "@/app/utils/types";
import { PsychGameState_t } from "./types";

export async function addPsychTextInput(text: UserText_t, state: PsychGameState_t) {
    state:= await fetchWithType < Psych
}